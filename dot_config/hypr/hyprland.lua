require("utils")
require("env")
require("animations")
require("autostart")
require("bindings")
require("config")
require("input")
require("monitors")
require("rules")

require("dms/colors")

hl.config({
	group = {
		col = {
			border_active = "rgb(295675)",
		},
		groupbar = {
			col = {
				active = "rgb(295675)",
			},
		},
	},
})
